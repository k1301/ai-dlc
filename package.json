{
  "name": "table-order-service",
  "version": "1.0.0",
  "description": "Table Order Service - Database and Backend",
  "main": "index.js",
  "scripts": {
    "db:migrate": "node scripts/migrate.js",
    "db:rollback": "node scripts/rollback.js",
    "db:seed": "node scripts/seed.js",
    "db:reset": "npm run db:rollback && npm run db:migrate && npm run db:seed"
  },
  "keywords": [
    "table-order",
    "restaurant",
    "pos"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "knex": "^3.0.1",
    "pg": "^8.11.3",
    "bcrypt": "^5.1.1"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
